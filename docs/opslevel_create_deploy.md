## opslevel create deploy

Create deployment events

### Synopsis

Create deployment events

```
opslevel create deploy [flags]
```

### Options

```
      --commit-message string    git commit message associated with the event (OPSLEVEL_DEPLOYER_EMAIL)
      --commit-sha string        git commit sha associated with the event (OPSLEVEL_DEPLOYER_NAME)
      --dedup-id string          dedup id of the event (OPSLEVEL_DEDUP_ID)
      --deploy-number string     deploy number of the event (OPSLEVEL_DEPLOY_NUMBER)
      --deploy-url string        url the event will link back to (OPSLEVEL_DEPLOY_URL)
      --deployer-email string    deployer email who created the event (OPSLEVEL_DEPLOYER_EMAIL)
      --deployer-name string     deployer name who created the event (OPSLEVEL_DEPLOYER_NAME)
  -d, --description string       description of the event (OPSLEVEL_DESCRIPTION)
      --dry-run                  if true data will be logged and not sent to the integration-url (OPSLEVEL_DRY_RUN)
      --environment string       environment name of the event (OPSLEVEL_ENVIRONMENT)
      --git-path string          relative path to grab the git commit info from (if git repo is found overrides all commit details) (default "./")
  -h, --help                     help for deploy
  -i, --integration-url string   OpsLevel integration url (OPSLEVEL_INTEGRATION_URL)
  -s, --service string           service alias for the event (OPSLEVEL_SERVICE)
```

### Options inherited from parent commands

```
      --api-token string    The OpsLevel API Token. Overrides environment variable 'OPSLEVEL_API_TOKEN'
      --api-url string      The OpsLevel API Url. Overrides environment variable 'OPSLEVEL_API_URL' (default "https://api.opslevel.com/graphql")
      --app-url string      The OpsLevel App Url. Overrides environment variable 'OPSLEVEL_APP_URL' (default "https://app.opslevel.com")
  -f, --file string         File to read data from. If '.' then reads from './data.yaml'. Defaults to reading from stdin. (default "-")
      --log-format string   overrides environment variable 'OPSLEVEL_LOG_FORMAT' (options ["JSON", "TEXT"]) (default "TEXT")
      --log-level string    overrides environment variable 'OPSLEVEL_LOG_LEVEL' (options ["ERROR", "WARN", "INFO", "DEBUG"]) (default "INFO")
```

### SEE ALSO

* [opslevel create](opslevel_create.md)	 - Create resources or events from a file or stdin

###### Auto generated by spf13/cobra on 24-Mar-2022
